# PLAN — SubTrack

Как вести этот файл: работа идёт блоками сверху вниз. Блок считается завершённым, когда тесты и линтер зелёные, сделан git-коммит и отмечены чекбоксы. Порядок и состав блоков меняются только по согласованию с пользователем. Источник требований — `docs/SPEC.md` (номера разделов ниже — ссылки на него).

---

## Этап 1 — MVP

### Блок 1. Каркас проекта ✅
- [x] Scaffold: Next.js 15 (App Router) + TypeScript strict, Tailwind, shadcn/ui, базовый layout с навигацией и тёмной/светлой темой
- [x] Prisma-схема всех сущностей (§3), первая миграция
- [x] Docker Compose (app + postgres), `prisma migrate deploy` при старте контейнера
- [x] Сиды с реалистичными демо-данными: ~12 сервисов (все billing-модели и циклы, разные валюты), ~30 мест, платежи за 3 прошедших месяца
- [x] `.env.example` со всеми переменными, краткий README (запуск dev и compose)

**Готово, когда:** `docker compose up -d` поднимает приложение с демо-данными; `npm run dev` работает.

**Сделано (PR #1):**
- Каркас Next.js 15 (App Router, TS strict + `noUncheckedIndexedAccess`), Tailwind v3 + shadcn/ui (new-york, `button`/`card`), тема light/dark через `next-themes`, layout с боковой навигацией (Дашборд/Сервисы/Сотрудники/Отчёты/Настройки) и заглушки страниц.
- Prisma-схема всех сущностей §3 (включая таблицы Auth.js), деньги — `Decimal(18,6)`, курсы — `Decimal(18,8)`. Первая миграция `init` + вручную дописанный частичный уникальный индекс `seat_active_unique` (одно активное место на пару service+employee, §3.6).
- Настройки инстанса — singleton-модель `Setting` (baseCurrency, confirmationOverdueDays).
- Docker: `Dockerfile` (standalone-сборка), `docker-compose.yml` (postgres+app), `docker-entrypoint.sh` — `migrate deploy` + идемпотентный сид при старте (сид в контейнере через `node --experimental-strip-types`, т.к. tsx — devDependency).
- Сид: 12 сервисов (fixed/per_seat/hybrid × monthly/yearly, валюты USD/EUR/GBP), 30 мест (с демо-override цены), 15 сотрудников, 3 способа оплаты, 6 категорий, FxRate, платежи за 3 прошедших месяца. Идемпотентен (upsert по фикс-id), проверен повторным прогоном.
- `.env.example`, README (dev + compose), Vitest (+smoke-тест) и Playwright настроены.
- **Проверено:** `typecheck`, `lint`, `test`, `build` — зелёные; `migrate deploy` + `seed` на живой Postgres (12/30/30/15); `npm run dev` отдаёт 200 на `/` и `/services`.
- **Проверяет пользователь руками:** полный `docker compose up -d` (сборка образа app) — по агрегированной с пользователем договорённости про заглушки/секреты.

### Блок 2. Аутентификация и роли ✅
- [x] Auth.js v5 + Google provider
- [x] Whitelist `AllowedEmail`: вход только из списка; `ADMIN_EMAILS` из env сидятся при первом старте
- [x] Роли Viewer / Manager / Admin; серверные guard'ы на все мутации
- [x] Экран отказа в доступе с понятным сообщением

**Готово, когда:** выполняется критерий приёмки №8 (§9).

**Сделано (PR #2):**
- Auth.js v5 (`next-auth@beta`) + `@auth/prisma-adapter`, Google provider, JWT-сессии. Split-конфиг: `src/auth.config.ts` (edge-safe, для middleware) + `src/auth.ts` (Node, адаптер + callbacks). Route handler `/api/auth/[...nextauth]`.
- Whitelist: `signIn`-callback пускает только email из `AllowedEmail` (проверка на каждый вход → добавление в whitelist открывает доступ без перезапуска, критерий №8). `ADMIN_EMAILS` сидятся в whitelist на старте через `src/instrumentation.ts`. Первый вход копирует роль из whitelist в `User` (`events.createUser`, §3.1).
- Роли Viewer/Manager/Admin: чистый модуль `lib/roles.ts` (иерархия + `hasRole` + подписи) с unit-тестами; guard'ы `lib/authz.ts` (`requireUser`/`requireManager`/`requireAdmin`, `AuthorizationError`) — применяются к мутациям по мере их появления в след. блоках.
- `middleware.ts` защищает все маршруты (кроме `/login`, `/denied`, `/api/auth`, статики) с редиректом на `/login`. Route-group `(app)` с серверной проверкой доступа + shell (навигация, тема, меню пользователя с ролью и выходом). Публичные `/login` (Google-кнопка) и `/denied` — без shell.
- **Проверено:** `typecheck`/`lint`/`test` (7 тестов)/`build` — зелёные; на dev-сервере `/` и `/services` без сессии → 307 на `/login`; `/login` и `/denied` рендерятся; instrumentation засидил admin-whitelist на старте.
- **Проверяет пользователь руками:** реальный вход через Google (нужны `AUTH_GOOGLE_ID/SECRET`) — по договорённости про заглушки/секреты.
- ⚠️ Прим. по среде: порт 3000 у пользователя занят другим Docker-контейнером — dev/compose SubTrack поднимать на свободном порту.

### Блок 3. Справочники и настройки ✅
- [x] CRUD категорий (name, color) — Admin
- [x] CRUD способов оплаты (с архивацией) — Admin
- [x] Настройки инстанса: базовая валюта (по умолчанию USD), срок «просрочки» подтверждения (по умолчанию 5 дней)

**Сделано (PR #3):**
- Экран «Настройки» (`/settings`) с вкладками Категории / Способы оплаты / Параметры; доступ только Admin (серверный gate + пункт навигации скрыт для не-админов).
- CRUD категорий (name + color-picker, валидация #RRGGBB, защита от удаления используемой категории) и способов оплаты (создание/правка + **архивация** вместо удаления). Server actions с zod-валидацией, `revalidatePath`, тостами (sonner) и pending-состоянием.
- Параметры инстанса: базовая валюта (USD/EUR/GBP/UAH/PLN) и срок просрочки подтверждения (по умолчанию 5); singleton `Setting` через upsert.
- Все мутации защищены `requireAdmin` и пишут **аудит-лог** (`lib/audit.ts`: `writeAudit` + `buildDiff` old→new) — задел под §3.10 (UI-просмотр аудита — Этап 2).
- Добавлены UI-примитивы shadcn (input, label, badge, table, dialog, tabs, separator, toaster, submit-button), зависимости zod/sonner/radix.
- **Проверено:** `typecheck`/`lint`/`test` (11 тестов, +buildDiff)/`build` — зелёные; `/settings` без сессии → 307; dev-лог чистый.
- **Проверяет пользователь руками:** сам CRUD под admin-сессией (нужен Google-вход).

### Блок 4. Сервисы, сотрудники, места ✅
- [x] CRUD сервисов со всеми полями §3.4; favicon по `vendor_url` с fallback на инициалы
- [x] Таблица сервисов: поиск, фильтры (статус/цикл/категория/ответственный/способ оплаты), сортировка, сохранённые пресеты
- [x] Карточка сервиса: вкладки Обзор / Места / Платежи (вкладка «История» — Этап 2)
- [x] Сотрудники: таблица с суммой стоимости мест, карточка; автосоздание сотрудника по новому email при добавлении места
- [x] Места: добавление с автокомплитом по email, override цены места, закрытие с undo, валидация «одно активное место на пару сервис+сотрудник»
- [x] Запись аудит-лога на все изменения (UI-просмотр — Этап 2)

**Сделано (PR #4):**
- CRUD сервисов (server actions, Manager+) со всеми полями §3.4 и условной валидацией (zod + superRefine: per_seat→price=0, fixed→seatPrice=null, monthly→billingDay, yearly→renewalDate); авто-`next_payment_date`. Форма-диалог с условной видимостью полей.
- Таблица сервисов: поиск, фильтры (статус/цикл/категория/ответственный/способ оплаты), сортировка по колонкам, **сохранённые пресеты фильтров** (localStorage), колонка «стоимость/мес норм.» и favicon.
- Favicon-компонент (Google s2 по `vendor_url`, fallback на инициалы).
- Карточка сервиса: вкладки **Обзор / Места / Платежи**; правка и архивация из шапки.
- Места: добавление с **автокомплитом email** (datalist), автосоздание сотрудника по новому email (в транзакции), override цены, **закрытие с undo** (тост «Отменить» → reopen), валидация «одно активное место на пару» (частичный индекс + проверка в коде).
- Сотрудники: таблица с суммой стоимости мест/мес (мультивалютная разбивка) и карточка (правка имени/отдела, места с стоимостью).
- Расчёт: чистый модуль `lib/calc/service-cost.ts` (стоимость §4.1 + run-rate) и `lib/calc/dates.ts` (clamp billing_day, next-payment) с unit-тестами (частично из задела Блока 5). Утилиты `lib/format.ts` (деньги ru-RU, даты DD.MM.YYYY).
- Аудит-лог пишется на все мутации Service/Seat/Employee.
- **Проверено:** `typecheck`/`lint`/`test` (25 тестов)/`build` — зелёные; скрипт против живой БД подтвердил расчёты (Figma override=65, Adobe yearly=110, Sentry hybrid GBP=56, next-payment-даты, мультивалютные суммы сотрудников).
- **Проверяет пользователь руками:** сам UI CRUD под сессией.
- Прим.: run-rate и суммы показываются **в валюте сервиса**; пересчёт в базовую валюту — Блок 6.

### Блок 5. Расчётный модуль `lib/calc` — сначала тесты ✅
- [x] Unit-тесты: стоимость сервиса (fixed / per_seat / hybrid) — §4.1
- [x] Unit-тесты: нормализация yearly → monthly (run-rate)
- [x] Unit-тесты: сборка плана месяца — §4.2, включая `billing_day` 29–31 в коротких месяцах и февраль
- [x] Unit-тесты: конвертация валют; отсутствие курса на дату → последний известный до неё
- [x] Реализация чистых функций до полностью зелёных тестов

**Сделано (PR #5):**
- `lib/calc/fx.ts`: `findRate`/`convert` — курс `from→to` на дату (§3.9) с правилом «последний известный до даты», инверсия обратного курса, identity для одной валюты, null при отсутствии.
- `lib/calc/plan.ts`: `expectedDateForMonth`/`buildPlanLine`/`buildMonthlyPlan` — сборка плана месяца (§4.2): какие сервисы попадают, дата (с clamp billing_day), сумма за цикл (§4.1) и breakdown (фикс/кол-во мест/сумма мест); yearly — только в месяц продления целиком.
- Стоимость (§4.1) и run-rate уже покрыты в Блоке 4 (`service-cost.ts`), даты — `dates.ts`.
- **Тесты (46 всего):** billing_day 31 в феврале → 28.02 (критерий №7), 29.02 в високосный 2028, 31 в апреле → 30; yearly $1200 только в сентябре (критерий №2); EUR→USD по курсу даты и «нет курса → последний известный» (критерий №5); fixed/per_seat/hybrid суммы и breakdown; неактивные сервисы вне плана.
- **Проверено:** `typecheck`/`lint`/`test` (46)/`build` — зелёные.

### Блок 6. Курсы валют ✅
- [x] `FxRate`: загрузка с frankfurter.app, cron ежедневно 07:00 `APP_TZ`
- [x] Ручной ввод/правка курса в настройках (fallback)

**Сделано (PR #6):**
- `lib/fx/frankfurter.ts` — загрузка курсов base→symbols с frankfurter.app (ЕЦБ); `lib/fx/update-rates.ts` — `updateFxRates()` сохраняет `foreign → base` (инверсия) в `FxRate`, идемпотентно по (date, from, to), база из настроек.
- Cron `croner` в `instrumentation.ts`: ежедневно **07:00 `APP_TZ`** (гард от двойного планирования при hot-reload). Задел под план-снапшот cron (Блок 7) — там же.
- Вкладка **«Курсы валют»** в настройках (Admin): таблица актуальных курсов (последний известный по паре), кнопка **«Обновить сейчас»** (ручной запуск) и форма ручного ввода/правки курса (fallback). Аудит на ручные правки и обновления.
- **Проверено:** `typecheck`/`lint`/`test` (46)/`build` — зелёные; **живой запрос к frankfurter.app** записал EUR→USD 1.14, GBP→USD 1.34, PLN→USD 0.26 на 2026-07-22 в БД (инверсия к базовой USD корректна).
- Прим.: UAH ЕЦБ/frankfurter не отдаёт — вводится вручную (на то и fallback).

### Блок 7. План-снапшоты ✅
- [x] Генерация снапшота: cron 1-го числа 00:05 `APP_TZ`; ручная пересборка Admin'ом с записью в аудит-лог
- [x] `PlanLine` с breakdown (фикс-часть, количество и сумма мест); неизменяемость плана задним числом — §3.8
- [x] «Прогноз до конца месяца» по живым данным

**Сделано (PR #7):**
- `lib/plan/generate-snapshot.ts` — `generateSnapshot()`: собирает план месяца (Блок 5) по active-сервисам с активными местами, `amountBase` по курсу даты снапшота (Блок 6), `PlanLine` с breakdown (фикс/кол-во/сумма мест). **Неизменяемость §3.8**: если снапшот месяца есть и нет `force` — не трогаем; `force` = ручная пересборка.
- Cron `croner`: **1-го числа 00:05 `APP_TZ`** (создаёт снапшот нового месяца, `instrumentation.ts`).
- Ручная пересборка (`lib/actions/snapshots.ts`, Admin) с записью в аудит-лог; кнопка во вкладке «Планы» настроек.
- «Прогноз до конца месяца» — `lib/plan/forecast.ts`: чистая `sumRemainingBase` (+тесты) и live `forecastToEndOfMonth` (план по живым данным, остаток от `asOf`). Показывается во вкладке «Планы».
- **Проверено:** `typecheck`/`lint`/`test` (50)/`build` — зелёные; live-скрипт: снапшот июля = 10 строк (yearly Adobe/Ahrefs исключены), повтор без force → skipped (неизменяемость), force → пересборка, конвертация Notion 16 EUR→18.25 USD (курс 1.14), breakdown и прогноз корректны.
- Прим.: warning сборки про `jose`/Edge (`CompressionStream`) — из Auth.js-middleware, неиспользуемая ветка, безобиден (есть с Блока 2).

### Блок 8. Факт: платежи и подтверждение ✅
- [x] Лента «Ожидаемые списания»: ✓ создаёт Payment; диалог корректировки суммы/даты (дельта — в отчёт)
- [x] Ручное добавление платежа вне плана
- [x] Пометка строки плана `waived` с комментарием
- [x] `amount_base` фиксируется по курсу даты платежа

**Сделано (PR #8):**
- Миграция `planline_comment`: поле `PlanLine.comment` (для waived).
- `lib/actions/payments.ts` (Manager+): `confirmExpectedPayment` (создаёт Payment из строки плана, переводит строку в `confirmed`, диалог корректировки суммы/даты → дельта в отчёт), `recordManualPayment` (разовый платёж вне плана), `waivePlanLine` (строка → `waived` с комментарием). Все с аудитом.
- **`amount_base` фиксируется по курсу даты платежа** (§3.7) через `convert(...)` на `paidAt`.
- `lib/plan/expected-charges.ts` — лента ожидаемых списаний с флагом просрочки (`expectedDate + confirmationOverdueDays < today`, §4.3).
- UI: лента «Ожидаемые списания» на дашборде (✓ Подтвердить с диалогом / «списания не было» waived); кнопка «Добавить платёж» во вкладке «Платежи» карточки сервиса.
- **Проверено:** `typecheck`/`lint`/`test` (50)/`build` — зелёные; live-скрипт: 10 ожидаемых (9 просрочено), amount_base EUR→USD (41.6→47.46 по курсу), подтверждение создаёт платёж + `confirmed`, корректировка суммы даёт дельту +5 (критерий №4).

### Блок 9. Дашборд ✅
- [x] KPI-карточки: run-rate/мес, факт vs план текущего месяца с прогресс-баром, прогноз до конца месяца, активные сервисы/места
- [x] Блок «Требует внимания»: просроченные подтверждения + годовые в окне решения — §4.4
- [x] График 12 месяцев план/факт, донат по категориям, топ-5 сервисов по стоимости

**Сделано (PR #9):**
- `lib/dashboard/metrics.ts` — все метрики в базовой валюте (конвертация FX): run-rate/мес, факт/план текущего месяца + прогресс, прогноз, кол-во активных сервисов/мест, «требует внимания», ряды для графиков.
- KPI-карточки + прогресс-бар «факт vs план».
- Блок **«Требует внимания»** §4.4: просроченные подтверждения (лента) + годовые **в окне решения** (`renewal − notice ≤ today ≤ renewal`).
- Графики (Recharts): **12 месяцев план/факт** (BarChart), **донат по категориям** (run-rate), **топ-5** сервисов (пропорциональные полосы). Тема-aware через CSS-переменные.
- **Проверено:** `typecheck`/`lint`/`test` (50)/`build` — зелёные; live-скрипт: run-rate 1901.65 USD, 12/30 сервисов/мест, план июля 1626, факт по Apr/May/Jun ≈1614, 9 просрочек, категории (Infra 1335…) и топ-5 (AWS 1200…) корректны.

### Блок 10. Отчёт «План / Факт» ✅
- [x] Выбор месяца; итоги (план/факт/дельта/%); таблица по сервисам с авто-пометками причин дельт — §4.5
- [x] Срезы по категориям и ответственным; события месяца
- [x] Переключатель представлений: кэш-флоу / нормализованное
- [x] Экспорт CSV (UTF-8 with BOM)

**Сделано (PR #10):**
- `lib/report/monthly-report.ts` — отчёт за месяц: итоги (план/факт/дельта/%), таблица по сервисам с **авто-причинами дельт** (совпадает / новый-вне-плана / не подтверждено / waived / изменение суммы-мест / курсовая разница), срезы по категориям и ответственным, события месяца (новые подписки, добавленные/закрытые места, изменения цен из аудита).
- Переключатель **кэш-флоу / нормализованное** (yearly ÷ 12).
- Страница `/reports`: выбор месяца, тумблер представления, итоговые карточки, таблица с бейджами причин, срезы, события.
- **Экспорт CSV (UTF-8 with BOM)**: `lib/report/csv.ts` + роут `/api/reports/export` (Bearer сессии), `;`-разделитель, кириллица в Excel не бьётся.
- **Проверено:** `typecheck`/`lint`/`test` (50)/`build` — зелёные; live-скрипт: отчёт **сходится** (Σсервисов = итог, критерий №6), июль план 1625.82/факт 0 (причина «не подтверждено»), июнь факт 1613.58 (причина «новый»); CSV начинается с BOM (feff).

### Блок 11. CSV и финал этапа ✅
- [x] Импорт services и seats: загрузка → маппинг колонок → предпросмотр → дедупликация → импорт
- [x] CSV-экспорт из таблиц сервисов/сотрудников/платежей
- [x] e2e smoke (Playwright): создать сервис → добавить места → собрать снапшот → подтвердить платёж → открыть отчёт
- [x] Прогон всех «Критериев приёмки» (§9) → чек-лист пользователю: что готово, как проверить руками, что отложено

**Сделано (PR #11):**
- CSV-импорт services и seats: `lib/csv/parse.ts` (парсер с кавычками, автоопределение `;`/`,`) + `lib/csv/dedup.ts` (`partitionUnique`, дубли existing/in-file) — **с unit-тестами (§7)**; server actions `importServicesCsv`/`importSeatsCsv` (Manager+, автосоздание сотрудника, аудит); диалог с предпросмотром и авто-маппингом колонок.
- CSV-экспорт таблиц (`lib/csv/table-export.ts` + `/api/export?kind=services|employees|payments`), UTF-8 with BOM; кнопки на страницах сервисов/сотрудников/отчётов.
- **e2e smoke (Playwright)**: полный приёмочный сценарий сервис → место → снапшот → платёж → отчёт через реальный UI + негативный тест whitelist. Тестовый вход — env-защищённый Credentials-провайдер (`E2E_TEST_AUTH=1`, в проде отсутствует). **2/2 passed.**
- **Проверено:** `typecheck`/`lint`/`test` (56 unit)/`build`/`test:e2e` (2) — всё зелёное; экспорт с BOM, дедуп импорта на живой БД (Miro unique, Figma existing, Miro in-file).

---

## Критерии приёмки (§9) — результат прогона

| # | Критерий | Статус | Как проверено / где |
|---|---|---|---|
| 1 | per-seat 3×$10 → +$30/мес; закрытие места → +$20 | ✅ | unit `service-cost.test`; дашборд run-rate; e2e |
| 2 | yearly $1200 (15.09): норм. $100/мес; кэш-флоу $1200 в сент. | ✅ | unit `plan.test`, `service-cost.test`; тумблер отчёта |
| 3 | место 10-го не меняет план тек. месяца, видно в прогнозе + план след. | ✅ | неизменяемость снапшота §3.8 + live-прогноз (Блок 7) |
| 4 | подтверждение в 1 клик → Payment+confirmed; скорр. сумма → дельта | ✅ | e2e; live-скрипт (дельта +5) |
| 5 | EUR при базовой USD по курсу даты; курсовая разница как причина | ✅ | unit `fx.test`; amount_base по курсу; причина «fx» в отчёте |
| 6 | отчёт сходится (Σ=итог); CSV в Excel без битой кириллицы | ✅ | live-скрипт (сходимость); CSV с BOM |
| 7 | billing_day=31 в феврале → 28/29 | ✅ | unit `plan.test` (28.02 и 29.02 високосный) |
| 8 | вход вне whitelist — отказ; добавление email — доступ без рестарта | ✅ | signIn-callback (проверка на каждый вход); e2e негативный тест |
| 9 | MCP whoami / create_service Viewer→403 | ⏭️ Этап 2 | MCP-сервер — Этап 2 |
| 10 | офбординг 4 места одним действием | ⏭️ Этап 2 | офбординг-флоу §4.6 — Этап 2 |
| 11 | изменение цены в истории с автором old→new | ◑ частично | аудит old→new **пишется** (buildDiff); UI-просмотр истории — Этап 2 |

**Итог Этапа 1:** критерии 1–8 выполнены полностью; 9–10 относятся к Этапу 2; по 11 данные аудита фиксируются, UI-просмотр — Этап 2.

**Как проверить руками (нужны Google OAuth-секреты и запуск):**
1. Заполнить `.env` (`AUTH_GOOGLE_ID/SECRET`, `ADMIN_EMAILS`), `docker compose up -d` (порт 3000 у вас занят — поменяйте маппинг или освободите).
2. Войти своим Google-аккаунтом из `ADMIN_EMAILS`; проверить отказ для чужого email.
3. Дашборд → «Ожидаемые списания» → подтвердить/waived; Сервисы → создать/править, импорт/экспорт CSV; Отчёты → месяц, тумблер, экспорт; Настройки → справочники, курсы («Обновить сейчас»), пересборка снапшота.

---

## Этап 2 — начинать только после явного подтверждения пользователя

- [ ] REST API `/api/v1/*`; API-токены: имя + роль, хэш в БД, показ значения один раз
- [ ] MCP-сервер на `/mcp` (Streamable HTTP, Bearer): полный набор инструментов §6
- [ ] UI аудит-лога: общий журнал (Admin) + вкладка «История» в карточке сервиса
- [ ] ⌘K-палитра: поиск сервисов и быстрые действия
- [ ] Офбординг-флоу сотрудника — §4.6
- [ ] CSV-импорт платежей

## Этап 3 — идеи вне скоупа (не делать без отдельного решения)
- Синхронизация сотрудников из Google Workspace Admin SDK
- Детект неиспользуемых мест через API вендоров